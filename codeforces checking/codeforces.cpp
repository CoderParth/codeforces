#include <bits/stdc++.h>
using namespace std;

int main() {
    // use a hashmap
    int t;
    cin >> t;

    set<char>  = {
        'c', 'o', 'd', 'e', 'f', 'r', 's'
    };

    while(t--) {
        char c;
        cin >> c;

        if(set.count(c)) {
            cout << "YES" << endl;
        } else {
            cout << "NO" << endl;
        }
    }
    return 0;
}